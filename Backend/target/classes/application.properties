# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dt62zwpa3}
cloudinary.api-key=${CLOUDINARY_API_KEY:724288582118731}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:laxjw5VSu--uiiOmsudev1TtEwE}

# Server Configuration - Use Railway's PORT
server.port=${PORT:8080}
spring.application.name=ajarly-backend

# Database Configuration - Use Railway environment variables
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?zeroDateTimeBehavior=convertToNull&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# JPA/Hibernate Configuration - PRODUCTION SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:ajarly_secret_key_2025_change_this_in_production_environment}
jwt.expiration=${JWT_EXPIRATION:604800000}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.file-size-threshold=2MB

logging.level.root=WARN
logging.level.com.ajarly.backend=INFO
logging.level.org.hibernate=ERROR
logging.level.org.springframework=WARN
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Payment Gateway Configuration
fawry.enabled=${FAWRY_ENABLED:false}
fawry.merchant.code=${FAWRY_MERCHANT_CODE:TEST_MERCHANT_CODE}
fawry.security.key=${FAWRY_SECURITY_KEY:TEST_SECURITY_KEY}
fawry.api.url=${FAWRY_API_URL:https://atfawry.fawrystaging.com}
fawry.webhook.secret=${FAWRY_WEBHOOK_SECRET:TEST_WEBHOOK_SECRET}

# Platform Fees
payment.platform.fee.percent=10.00
payment.fawry.fee.percent=2.75

# Payment Settings
payment.auto.confirm=true
payment.refund.auto.approve=true
payment.currency=EGP

# Webhook URLs
payment.webhook.base.url=${WEBHOOK_BASE_URL:https://ajarly-backend-production.up.railway.app/api/v1/webhooks}

# Static Resources
spring.mvc.log-resolved-exception=false
spring.web.resources.add-mappings=true
logging.level.org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver=ERROR

# Google OAuth Configuration
google.client.id=${GOOGLE_CLIENT_ID:394475687795-fmaq9c9qd18t6uceu8mge409f2fs6r1s.apps.googleusercontent.com}